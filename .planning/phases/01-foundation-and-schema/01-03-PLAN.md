---
phase: 01-foundation-and-schema
plan: 03
type: execute
wave: 1
depends_on:
  - "01-01"
files_modified:
  - src/app/layout.tsx
  - src/app/login/page.tsx
  - src/components/app-sidebar.tsx
autonomous: true
requirements:
  - FOUND-05

must_haves:
  truths:
    - "Browser tab title shows EduDash"
    - "Login page heading reads EduDash, not Song Tool"
    - "Sidebar header shows EduDash"
    - "Sidebar navigation links point to /dashboard and /students, not /songs or music routes"
    - "No remaining 'Song Tool' text visible in the rendered app"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Updated metadata title and description"
      contains: "EduDash"
    - path: "src/app/login/page.tsx"
      provides: "Updated heading, subtitle copy, and icon"
      contains: "GraduationCap"
    - path: "src/components/app-sidebar.tsx"
      provides: "Updated header text and nav items"
      contains: "EduDash"
  key_links:
    - from: "src/app/layout.tsx"
      to: "browser tab"
      via: "metadata.title"
      pattern: "title.*EduDash"
    - from: "src/components/app-sidebar.tsx"
      to: "sidebar header"
      via: "span or text content"
      pattern: "EduDash"
---

<objective>
Replace all "Song Tool" legacy branding with "EduDash" across the three files that contain it. Update nav items in the sidebar to point to EduDash routes.

Purpose: The app currently identifies itself as "Song Tool" to users. This must be corrected before any real users or stakeholders interact with it.
Output: Three modified files. Zero remaining "Song Tool" references in the rendered interface.
</objective>

<execution_context>
@C:/Users/Hector/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-schema/01-RESEARCH.md
</context>

<interfaces>
<!-- Rebrand scope from RESEARCH.md — exact changes documented. -->

src/app/layout.tsx changes:
  metadata.title: "Song Tool" → "EduDash"
  metadata.description: update to "Student aggregator dashboard for educators"

src/app/login/page.tsx changes:
  <h1>Song Tool</h1> → <h1>EduDash</h1>
  subtitle copy → education-appropriate
  Music icon import → GraduationCap (available in lucide-react@0.577.0)
  router.push("/songs") → "/dashboard" (already done in Plan 02, but verify)

src/components/app-sidebar.tsx changes:
  Header <span>Song Tool</span> → <span>EduDash</span>
  Nav items replace music routes with:
    { title: "Dashboard", url: "/dashboard", icon: LayoutDashboard }
    { title: "Students", url: "/students", icon: Users }
  (Both /dashboard and /students are stubs for now — they just must not point to /songs etc.)
  Icons LayoutDashboard and Users are both available in lucide-react@0.577.0
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update metadata and login page copy</name>
  <files>src/app/layout.tsx, src/app/login/page.tsx</files>
  <action>
Read both files first.

**`src/app/layout.tsx`:**
Find the `metadata` export object. Change:
- `title` value from `"Song Tool"` (or whatever the current value is) to `"EduDash"`
- `description` value to `"Student aggregator dashboard for educators"`
No other changes to this file.

**`src/app/login/page.tsx`:**
Three changes:
1. Find `import { Music } from "lucide-react"` (or similar music icon import) and change to `import { GraduationCap } from "lucide-react"`. If the file imports multiple icons, only change the `Music` import. `GraduationCap` is confirmed available in lucide-react 0.577.0.
2. Find the `<h1>` tag that contains "Song Tool" and change its content to "EduDash".
3. Find any subtitle/description paragraph text that references music or "Song Tool" and change to education-appropriate copy. Suggested: "Sign in to your EduDash account" or "Student data dashboard for educators".
4. Replace the `<Music ... />` JSX element with `<GraduationCap ... />` (same props — className, size, etc.).

Note: `router.push("/songs")` may have already been changed by Plan 02. If it reads `/dashboard` already, leave it. If it still reads `/songs`, change it now.
  </action>
  <verify>
    <automated>cd C:/Users/Hector/Documents/EduDashTool && grep -q "EduDash" src/app/layout.tsx && grep -q "GraduationCap" src/app/login/page.tsx && ! grep -q "Song Tool" src/app/layout.tsx && ! grep -q "Song Tool" src/app/login/page.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - `layout.tsx` metadata.title is "EduDash"
    - `login/page.tsx` h1 contains "EduDash", imports GraduationCap (not Music), no "Song Tool" text remaining
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebrand sidebar header and nav items</name>
  <files>src/components/app-sidebar.tsx</files>
  <action>
Read the file first. This file is the main navigation sidebar.

Four changes:

**1. Header text:**
Find the element rendering "Song Tool" in the sidebar header (likely a `<span>` or similar). Change its text content to "EduDash". Keep surrounding elements (logo placeholder, styling classes) intact.

**2. Nav items array:**
Find the navigation items array/config. It currently contains music-related routes (Songs, Discovery, Playlists, Metronome, Chord Pads or similar). Replace the entire nav items list with:
```typescript
const navItems = [
  { title: "Dashboard", url: "/dashboard", icon: LayoutDashboard },
  { title: "Students", url: "/students", icon: Users },
];
```
These routes do not need to be implemented yet — the sidebar just needs correct destinations for EduDash.

**3. Icon imports:**
Add `LayoutDashboard` and `Users` to the lucide-react import. Remove the music-related icon imports (AudioLines, Music, Guitar, etc.) that are no longer needed. Keep any non-music icons that remain in use (e.g., LogOut, ChevronDown, or any icons used outside the nav items list).

**4. Verify no remaining "Song Tool" references:**
After changes, search the file for "Song Tool" — there should be zero occurrences.

Do NOT change:
- The sign-out button logic
- The user email display logic
- The theme toggle
- The sidebar collapse/expand behavior
- The `app-sidebar.tsx` imports from `@/components/ui/sidebar`
  </action>
  <verify>
    <automated>cd C:/Users/Hector/Documents/EduDashTool && grep -q "EduDash" src/components/app-sidebar.tsx && grep -q "LayoutDashboard" src/components/app-sidebar.tsx && grep -q "Users" src/components/app-sidebar.tsx && ! grep -q "Song Tool" src/components/app-sidebar.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - Sidebar header renders "EduDash"
    - Nav items are Dashboard (/dashboard) and Students (/students)
    - No music icon imports, no "Song Tool" text
    - `npm run dev` starts without TypeScript errors in this file
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -r "Song Tool" src/` returns no results
2. `npm run dev` compiles without errors
3. Browser: tab title shows "EduDash"
4. Browser: login page h1 shows "EduDash", GraduationCap icon visible
5. Browser: sidebar header shows "EduDash", nav shows Dashboard and Students links
6. Run `npm run test:e2e -- --grep "rebrand"` — test passes
</verification>

<success_criteria>
- Zero "Song Tool" occurrences in `src/app/layout.tsx`, `src/app/login/page.tsx`, `src/components/app-sidebar.tsx`
- Sidebar nav items point to /dashboard and /students (not music routes)
- E2E rebrand test passes: page title contains "EduDash", h1 does not contain "Song Tool"
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-schema/01-03-SUMMARY.md`
</output>
